Progettazione di Algoritmi – Esonero

This folder contains the report for the **Progettazione di Algoritmi** (Algorithm Design) course, part of my Bachelor's degree.  
The report is written in Italian, as the course and its assignments were held in Italian.

📘 Project Description

The project consists of designing an algorithm capable of counting the number of paths with at most *k* turns from the top-left cell to the bottom-right cell of an *n × n* matrix.

Below is the original problem statement (in Italian):

---

📝 Traccia (Original Assignment)

Vogliamo contare il numero di cammini con al più *k* svolte che partono dalla  
cella in alto a sinistra e raggiungono la cella in basso a destra in una matrice  
*n × n*.

Nel corso di un cammino dalla generica cella *(i, j)* ci si può spostare nella cella  
in basso (vale a dire la cella *(i + 1, j))* o nella cella a destra (vale a dire la cella  
*(i, j + 1))*.

Un movimento è considerato una svolta se ci si stava spostando lungo una riga  
e poi ci si sposta lungo una colonna, o viceversa.

Progettare un algoritmo che risolve il problema in tempo **O(n²k)**.

---

📎 Contents

- `Relazione_Esonero_Scarselli_1918975.pdf` — Main report (in Italian)
- `v1_recursion.py` — First version (pure recursion)
- `v2_memoization.py` — Second version (DP with memoization)
- `v3_combinatorial.py` — Third version (combinatorial approach)


